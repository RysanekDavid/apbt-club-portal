import { Typography, Container } from "@mui/material";

const GalleryPage = () => {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        Galerie
      </Typography>
      <Typography variant="body1">Fotogalerie z akcí a setkání</Typography>
    </Container>
  );
};

export default GalleryPage;
